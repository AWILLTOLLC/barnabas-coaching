# 2026-09-05

- Local model cleanup: deleted gemma4 (9.6GB) + Qwen3.8-27B UD-Q6_K (22GB). Kept uncensored Q8 (rare use, Aaron OK with model swap). Remaining local: quinn-q8 (37GB, LittleJohn), Jackrong Q4_K_M (17GB, Quinn Discord), HauhauCS uncensored Q8 (37GB).
- Flash Next (177B MoE ~100GB Q4) evaluated: too big for 128GB daily use. Revisit at 256GB.
- **Dru model switch:** main session now `openrouter/z-ai/glm-5.3-flash` ($0.07/$0.25, coding 71.5, agentic 51.5, multimodal: image/video). Config default still v4-pro. ModelPolicy requires full provider prefix (`openrouter/z-ai/glm-5.3-flash`) and needed gateway restart to take effect.
- v4-flash is text-only — proved empirically when Aaron sent an image I couldn't read.
- **LittleJohn registered as real agent** (`agents.entries.littlejohn`, ollama/quinn-q8:latest, workspace `agents/littlejohn/`). Confirmed via session history his earlier Robinhood research ran on quinn-q8 (api: ollama), not my cloud model.
- **Cross-agent messaging enabled:** `tools.agentToAgent.enabled: true` + `tools.sessions.visibility: all`. Verified: pinged Frankie, got reply. Alert path: any agent → sessions_send to Dru → Discord relay.
- **Discord relay nuance:** all relayed DMs appear under Quinn's bot identity (she owns the token); actual sender named in message body. Aaron = erasei, ID 276104854303145994, on DM allowlist. conversationRef conv_1e53bbbbb69c38e1054944d8c8203eec.
- **Dashboard started:** "Fleet Overview" tab with 4 session:progress cards (main, marketing, qwen, littlejohn). Custom HTML widgets (roster table, Ollama status) need show_widget — unavailable this turn, flagged as follow-up. /dashboards page only lists threads whose preferred face is Dashboard.
- OpenRouter multimodal landscape researched: GLM 5.3 Flash cheapest+strong; Gemini 3.8 Flash best quality; DeepSeek V4 Flash Vision Exp same-family option.
- LLM cost audit skill installed earlier this week; not automatic, invoke on demand.

## Fleet recency widget (dashboard lane session)

- show_widget still not exposed to agent toolset (main session client is Discord, no Activities; dashboard lane webchat didn't declare inline-widgets either).
- Workaround that worked: gateway RPC `board.widget.put {sessionKey:'agent:main:main', name:'fleet.recency', content:{kind:'html',html}, placement:{tabId:'fleet',size:'md'}, declared:{tools:['sessions.list']}}` via `openclaw gateway call`. Same underlying path as show_widget pin. Grant came back "granted" immediately (no Allow card).
- Data binding verified: `board.data.read` w/ viewTicket, bindingId sessions.list, params {activeMinutes:60,limit:200,includeDerivedTitles:true} → 10 sessions across 6 agents.
- Control UI chat deep link is `/chat?session=<sessionKey>` (agent derived from key) — NOT /chat/<agent>/<session>.
- Set Fleet tab chatDock right via `board.update` tab_update op.
