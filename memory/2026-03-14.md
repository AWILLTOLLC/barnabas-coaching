# Memory — 2026-03-14

## Nightly Consolidation

No session activity on March 13 or 14. MEMORY.md remains current and clean — no changes needed.

---

## Session Activity — 2026-03-14 (evening)

### Content Format Libraries Created
Maven tasked Dru to write execution-level content playbooks for all four business agents. Done:
- `channels/glimmer/references/content-formats.md` (Spark)
- `channels/morse-command/references/content-formats.md` (Dash)
- `channels/barnabas-coaching/references/content-formats.md` (Barrett)
- `channels/black-raven/references/content-formats.md` (Forge)

Each file: brand voice snapshot, audience primer, platform priority stack, 4–6 named post formats per platform with full templates + concrete examples, anti-patterns. Sourced from Maven's SOUL.md. Maven's MEMORY.md updated to reflect the files.

### Agent Commitment — Spark
Spark committed in the agents channel: after each content test, she'll post a quick note tagging @Maven with the result (killed / flopped / no signal). Maven will update the playbook file from that feedback. Goal: keep the content format libraries from going stale silently.

---

## Session Activity — 2026-03-14 (late evening, 22:45 UTC)

### SafeHarbor Architecture Finalized
Aaron confirmed lobster/crustacean naming theme, leaning SafeHarbor or Haven. Full architecture decided:
- **VM:** Alpine Linux via Virtualization.framework (macOS 13+, no Docker)
- **Runtime:** Single Node.js sidecar process for all agents
- **IPC:** vsock JSON-RPC 2.0 on port 5000 (Swift host ↔ Node.js guest)
- **Persistence:** SQLite via GRDB + 9p filesystem mounts
- **App Support:** `~/Library/Application Support/SafeHarbor/`
- **Guest mounts:** `/agents/` (agent configs), `/data/burrow.db` (SQLite)
- **API key:** passed as env var at VM boot
- **Bundler:** esbuild for TypeScript → JS
- Build plan: `reports/burrow-server-build-plan.md` (62KB, working title "Burrow" — rename pending)

### VantageOC Source Reviewed (VantageOC.zip → VantageSrc.zip)
Aaron provided two zips: first was compiled `.app` bundle (not source). Second (`VantageSrc.zip`) was the real Xcode source project.
- Complete Swift source (~60 files), SPM deps resolved
- **GRDB** already in use — aligns with build plan
- **Highlightr** for syntax highlighting
- `VANTAGE_V2_SPEC.md`, `specs/`, `docs/plans/` present
- `plugin/` dir has existing OpenClaw TypeScript plugin (gateway integration to be replaced)
- `.claude/skills/` with swiftui-pro and 18 other Claude Code skills
- **Key integration point:** Replace GatewayClient.swift + WebSocketConnection.swift + GatewaySSE.swift with SafeHarborVM.swift + VSockConnection.swift. Rest of UI/channel/agent model reusable.

### Orchestration Pattern — Long-Running Tasks
Discussed approach for 6-8 hour compute tasks that must run overnight without hitting rate limits:
- Task decomposition into DAG with token cost estimates
- Budget tracker reading Anthropic rate limit headers
- Stagger subagents to stay under rolling window limits
- State file (`tasks/project/todo.json`) persists progress across sessions
- Pattern worth implementing as a reusable orchestration skill

### brv curated
Saved SafeHarbor architecture to `.brv/context-tree/architecture/safe_harbor/safeharbor_architecture.md`
