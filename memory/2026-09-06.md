
## X2Claw Chrome extension (2026-09-06)

- Designed + built X2Claw: MV3 Chrome extension to send the current X post to an OpenClaw agent. Saved to projects/x2claw/ (manifest, background.js, common.js, popup.html/js, options.html/js, generated icon PNGs). Manifest JSON + node --check all pass.
- Design: activeTab + on-demand chrome.scripting (no persistent content script, no broad host perms); optional_host_permissions requested per-webhook-origin on Options save; POST runs in service worker with 15s AbortController; notification + inline status for results; agents/prefix/webhook/token/lastAgent in chrome.storage.local.
- Payload per Aaron's spec: {url, text, author, agent, prompt_prefix}. Options also carries a hook token (Authorization: Bearer).
- Gateway side discovered: real endpoint is Gateway HTTP hooks POST /hooks/agent + mapped hooks /hooks/<name> (docs/automation/cron-jobs.md). Disabled by default; needs hooks.enabled, dedicated token, allowedAgentIds. Receiver/mapping wiring NOT yet done. <!-- project: path:/Users/apollo/.openclaw/workspace -->
