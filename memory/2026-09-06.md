# 2026-09-06 — Session Log

## X2Claw Chrome Extension — DON'T USE
- Aaron didn't like it. Files deleted 2026-09-06. Idea archived only.
- Was: Chrome extension for sending X/Twitter posts to OpenClaw agents
- Original spec: browser action popup, tweet text extraction, agent dropdown, send button
- Ponytail review had cut 200 lines of scaffolding

## X Post Evaluation
- Evaluated Tom Bilyeu tweet: "5-person AI team using Claude → multi-million dollar business in 90 days"
- Verdict: Skip — funnel post, unfalsifiable claim, engagement bait → masterclass signup

## Wispr Flow Research
- Voice-to-text app by Wispr (raised $81M)
- Edits while you speak: removes fillers, catches mid-sentence corrections, formats text
- 90% zero-edit accuracy, 4x faster than typing
- Free tier is generous, worth trying

## Memory System Comparison (for harness training materials)
- Compared OpenClaw default vs our custom vs Hermes (Nous Research)
- **Key finding:** Our setup is richest but has two bugs:
  1. Four knowledge stores with no conflict resolution → first result wins regardless of trust
  2. Instincts are self-scored with no external grading → bias toward confident-but-wrong patterns

## Memory Fixes (BUILT ✅)
- **Fix 1: Provenance-weighted search** — `scripts/provenance_search.py`
  - Trust tiers: topics/wiki (1.0) > MEMORY.md (0.9) > daily notes (0.8) > situational-awareness (0.7) > dreaming (0.6) > ByteRover (0.5) > imports (0.4)
  - Tested: ByteRover at score 0.9 correctly demoted below MEMORY.md at 0.85
  - Agent applies inline after memory_search
- **Fix 2: Instinct reconciliation loop** — `scripts/reconcile_instincts.py`
  - Cross-references instincts against signals.jsonl corrections + ERRORS.md
  - Demotes contradicted instincts, flags zero-validation high-confidence ones
  - Tested: flagged 2 instincts, no demotions (signals.jsonl empty)
  - Wired into weekly Sunday 10pm PST cron alongside compress_signals.py

## Fern Agent Setup (Lily's Agent)
- Created agent `fern` with isolated workspace at `~/.openclaw/workspace/agents/fern/`
- Files: SOUL.md, USER.md, IDENTITY.md, AGENTS.md, MEMORY.md
- Model: Quinn Q8 local (no extra cost)
- Hard boundary: Fern cannot read Aaron's workspace or memory
- Added to openclaw.json agents.entries

## Lily Tailscale + Gateway Access
- Lily set up her own Tailscale account (lily.z.myers@gmail.com)
- Joined Aaron's tailnet (had to sign in under Aaron's identity, not create separate tailnet)
- Configured identityScopes in gateway auth: `lily.z.myers@gmail.com` → `["operator.read", "operator.write"]`
- No admin access, no role system (valid scope values don't support custom roles)
- Isolation enforced at agent/workspace level

## Config Changes
- openclaw.json: added Fern agent entry, added identityScopes for Lily
- Removed roles block (validation failed — custom role names not supported)
- Backup at openclaw.json.bak-fern

## Open Items
- X2Claw loader script build may not have completed (subagent was spawned but may have stalled)
- Gateway may need restart to pick up identityScopes changes
- Lily hasn't actually connected to the gateway yet (needs to open https://apollo-1.tailb4a099.ts.net and enter token)
- Fern agent hasn't been tested with an actual conversation
