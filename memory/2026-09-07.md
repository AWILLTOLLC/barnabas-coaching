# 2026-09-07

- Summarized @polydao X article on agent loops (via fxtwitter) — subagent task completed.
- Aaron asked for a plan to adopt the pattern into OpenClaw, focus on recurring self-improvement. Wrote `tasks/loop-engineering-plan.md`: Phase 0 CONSTRAINTS.md always-loaded corrections file; Phase 1 machine-evaluable stop conditions + agent-independent gates; Phase 2 alias table; Phase 3 weekly meta-loop (Review Agent proposes, Aaron approves, anti-drift rule: no self-editing approval/safety); Phase 4 state-driven cron dispatch; Phase 5 facts-before-edges. Deliberately not adopting: 300-agent swarms, unreviewed self-editing.

## Signals pipeline activation (12:05pm)

Aaron said "do it" → implemented ECC-comparison fixes:
- Nightly memory cron (2a848c7f) re-enabled (was disabled, 93 consecutive Discord delivery errors) with bestEffort delivery; signal extraction added as step 2 of its message.
- extract_signals.py: fixed /root→/Users sessions path, added SQLite fallback (transcript_events in agents/main/agent/openclaw-agent.sqlite, copy-to-tmp, unwrap type:"message" events), `from __future__ import annotations` for py3.9. Tested: 119 signals from current session (re_query=110 looks like false positives — word-overlap heuristic too loose, tune later).
- reconcile_instincts.py: numeric confidence (0.0-1.0, low=.35/med=.6/high=.85), atomic Trigger/Action format parsing (legacy still works), staleness decay (uncorroborated ≥8w + score<0.85 → demote one step). Dry-run: 2 demotions.
- AGENTS.md instinct template now atomic + `<!-- project: ... -->` tag convention.

## Barnabas configurator — Step 4 rewrite (13:48)
- Rewrote Step 4 of `projects/barnabas-coaching/configurator/agent-configurator.html` into "Hook it up to your agent": what-you-built, where-files-go (OpenClaw + Hermes harness-neutral), 6 guided steps with per-step one-liners + verification cues (new .command-row-desc/.command-verify CSS), verify + troubleshooting blocks. Existing commands and gating behavior preserved.
- Hermes verified: docs at hermes-agent.nousresearch.com (guides/use-soul-with-hermes, user-guide/features/personality) — `~/.hermes/SOUL.md` (or $HERMES_HOME) read at session start as slot #1 of system prompt; no IDENTITY.md concept.
- Synced to `public/agent-configurator.html` (logo → https://barnabas.coach). package.json has NO deploy script — nothing deployed.

## Barnabas configurator — Step 3/4 swap (14:xx)
- Swapped per Aaron: Export (harness install, was Step 4) is now Step 3 and comes first in DOM; Preview & Export (editors, copy/zip) is now Step 4. Progress bar: Soul, Identity, Export, Preview. Scroll after identity pick targets commandSection. All "Step 3" editor references updated to "Step 4". Files synced (public logo → barnabas.coach).
- Verified: node --check, 17/17 jsdom checks incl. harness toggle, zip packaging with edit-freshness, switchTab. Not deployed — Aaron/Barrett deploys.
