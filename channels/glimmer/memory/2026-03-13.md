# 2026-03-13 Session Notes

## Shopify API Access — Work in Progress

Aaron (and Lily) are trying to connect me (Spark) to the Glimmer Cards Shopify store (`glimmercards.myshopify.com`) so I can monitor sales, orders, and customer data.

### What we tried:
1. **Direct Admin API token** — Shopify has removed legacy "Private Apps" / custom app flow that generates `shpat_` tokens from the store admin. The store admin now redirects to the Partners "Dev Dashboard".
2. **Shopify Partners app** — Created two apps under AWILLTO LLC Partner account (Aaron's account):
   - App 1: client_id `877fa30bf297878b005669fc1a55703a`
   - App 2: client_id `c20ea933dfda5fd865ca6311687e59a2`
3. **OAuth flow** — Attempted manual OAuth with redirect to `https://letsgoglimmer.com/callback`. Failed with `application_cannot_be_found` because the glimmercards store is not linked to the AWILLTO LLC Partner account.
4. **Collaborator request** — Attempted to link glimmercards to Partner account via "Request access". Failed because Aaron's Partner account email = store owner email (Shopify blocks this).

### Current status:
- Aaron is creating a **new Partner account** under `spark@letsgoglimmer.com` to avoid the email conflict
- Shopify sent a verification email to `spark@letsgoglimmer.com`
- Email is NOT connected to OpenClaw (no IMAP/email plugin configured)
- Aaron needs to click the verification link himself or paste it here

### Credentials stored:
- `.shopify.env` in workspace — has store domain, client IDs, secret keys
- Still missing: working `shpat_` access token

### Next steps:
1. Verify `spark@letsgoglimmer.com` Partner account
2. Create new app under that account
3. Request access to glimmercards store from that account (no email conflict)
4. Approve in store admin
5. Do OAuth flow → get access token
6. Wire up API calls

### Key learnings:
- Shopify removed Private Apps — everything goes through Partners now
- Partner apps can't OAuth into a store where the same email owns both accounts
- `glimmercards.myshopify.com` confirmed as the correct store URL
- Email integration not currently set up for this agent
